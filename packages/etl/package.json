{
  "name": "@growth-os/etl",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "demo": "tsx src/demo.ts",
    "sync": "tsx src/sync.ts",
    "validate": "tsx src/validate.ts",
    "build": "tsc",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@growth-os/database": "workspace:*",
    "pino": "^8.19.0",
    "decimal.js": "^10.4.3",
    "date-fns": "^3.3.1",
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "@types/seedrandom": "^3.0.8",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.3.0"
  }
}
